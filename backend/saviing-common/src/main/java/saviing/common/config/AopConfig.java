package saviing.common.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AopConfig {
    // AspectJ CTW 활성화
    // 컴파일 타임에 바이트코드 조작으로 self-invocation 문제 해결
}